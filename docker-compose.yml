version: "3.8"

services:
  postgresql:
    image: postgres:16
    container_name: postgresql
    ports:
      - "5555:5432"
    environment:
      POSTGRES_DB: pizza-store
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

